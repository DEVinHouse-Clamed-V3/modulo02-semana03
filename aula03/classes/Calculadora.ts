abstract class Calculadora {
    static somar(a: number, b: number): number{
        return a + b;
    }
    static subtrair(a: number, b: number): number{
        return a - b;
    }
    static mulitplicar(a: number, b: number): number{
        return a * b;
    }
}

export default Calculadora